<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>cn.lingframe</groupId>
        <artifactId>lingframe-dependencies</artifactId>
        <version>${revision}</version>
        <relativePath>lingframe-dependencies</relativePath>
    </parent>

    <artifactId>LingFrame</artifactId>
    <packaging>pom</packaging>

    <name>LingFrame</name>
    <description>现代 JVM 微内核运行时治理框架</description>
    <url>https://github.com/LingFrame/LingFrame</url>

    <modules>
        <module>lingframe-dependencies</module>
        <module>lingframe-bom</module>
        <module>lingframe-api</module>
        <module>lingframe-core</module>
        <module>lingframe-runtime</module>
        <module>lingframe-infrastructure</module>
        <module>lingframe-examples</module>
        <module>lingframe-dashboard</module>
    </modules>

    <profiles>
        <!--
            性能基准测试模块（JMH）
            通过 -Pbenchmark 激活：
              mvn -pl lingframe-benchmark package -Pbenchmark -DskipTests
            支持 JDK 8+ 和 JDK 17+，CI 在两个版本上分别运行。
        -->
        <profile>
            <id>benchmark</id>
            <modules>
                <module>lingframe-benchmark</module>
            </modules>
        </profile>
        <!--
            中央仓库发布 Profile
            通过 -Prelease 激活：
              mvn clean deploy -Prelease -DskipTests
            自动生成 sources.jar, javadoc.jar, 执行 GPG 签名并推送到 Sonatype Central Portal
        -->
        <profile>
            <id>release</id>
            <properties>
                <skipTests>true</skipTests>
                <jacoco.skip>true</jacoco.skip>
                <spotbugs.skip>true</spotbugs.skip>
                <checkstyle.skip>true</checkstyle.skip>
            </properties>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.sonatype.central</groupId>
                        <artifactId>central-publishing-maven-plugin</artifactId>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-source-plugin</artifactId>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-javadoc-plugin</artifactId>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-gpg-plugin</artifactId>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-failsafe-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>flatten-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>com.github.spotbugs</groupId>
                <artifactId>spotbugs-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-checkstyle-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.sonatype.central</groupId>
                <artifactId>central-publishing-maven-plugin</artifactId>
                <extensions>true</extensions>
            </plugin>
        </plugins>
    </build>

</project>