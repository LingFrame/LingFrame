# 半年前我只知道"类加载器"这四个字，现在我做了一个 JVM 运行时治理框架

半年前，我对 ClassLoader 的全部了解就是"Java 有个东西叫类加载器"。没用过，没调过，更没写过。

现在，我做了一个基于三层 ClassLoader 隔离的 JVM 微内核运行时治理框架，叫**灵珑（LingFrame）**。核心代码大约十万行，包含运行时引擎、IntelliJ IDEA 静态分析插件、以及一个 AI 驱动的全栈产品 MVP。真正写代码的时间大概四个多月，大部分是业余时间。

这篇文章不是来推销产品的。我想聊聊这半年做了什么、怎么想的、以及一个没用过 ClassLoader 的人是怎么做出这些东西的。

---

## 为什么要做灵珑

做灵珑的起因很简单：我受够了微服务的运维成本。

一个中等规模的业务，拆成十几个微服务之后，你要维护十几套部署、十几个数据库连接池、十几个服务的监控告警、服务间的网络调用和超时重试。很多问题明明一个进程内就能解决，但大家选择了微服务，因为"隔离"和"独立部署"太重要了。

我就想：能不能在一个 JVM 进程内，实现模块级别的隔离和热加载？每个模块有自己的 ClassLoader、独立的生命周期、互不影响。出了问题单独回滚，不用重启整个服务。

后来我发现这个方向有个前辈叫 OSGi，但 OSGi 太重了、太老了，而且不提供治理能力（限流、熔断、权限、灰度）。Spring 的插件化方案也有类似的问题。于是我决定从零开始做。

---

## 灵珑的核心概念

灵珑的架构围绕一个核心抽象：**灵元（Ling）**。

一个灵元就是一个被治理的运行时单元。它可以是一个 Spring Boot 应用，也可以是一个纯 Java 模块。每个灵元有自己的 ClassLoader、自己的生命周期状态机、自己的治理策略（权限、限流、熔断）。灵元之间通过共享 API 层通信，实现类隔离但接口互通。

用一张图来理解灵珑的分层：

```
宿主应用 (LingCore)
    │
    ▼
灵珑运行时 (LingFrame Runtime)
    │
    ├── 灵元A (ClassLoader A, 独立生命周期, 独立治理策略)
    ├── 灵元B (ClassLoader B, 独立生命周期, 独立治理策略)
    └── 灵元C (ClassLoader C, 独立生命周期, 独立治理策略)
```

---

## 架构设计中的几个关键决策

### 1. 双层状态机

这是我觉得灵珑架构里最有意思的设计。

每个灵元实例有自己的状态机：**CREATED → LOADING → STARTING → READY → STOPPING → DEAD**，加上 ERROR 和 RECOVERING 两个异常分支。这是"事实层"——描述这个实例现在到底处于什么状态。

同时，每个灵元在宏观层面还有一个运行时状态机：**INACTIVE → ACTIVE ↔ DEGRADED → RECOVERING → STOPPING → REMOVED**。这是"意图层"——描述这个灵元对外表现出什么行为。

两层之间不直接修改对方。实例层产生状态变更事件，运行时层订阅这些事件，维护快照表，独立评估宏观状态。一个实例挂了不会直接拖垮运行时状态——运行时层会综合所有实例的情况再做判断。

```
实例层 (事实)          运行时层 (意图)
┌─────────┐           ┌──────────┐
│ READY   │──event──→│          │
│         │           │  ACTIVE  │
│ ERROR   │──event──→│          │──evaluate──→ DEGRADED
│         │           │          │
│ DEAD    │──event──→│          │──evaluate──→ STOPPING → REMOVED
└─────────┘           └──────────┘
```

这个设计的好处是：当你卸载一个灵元时，进入 STOPPING 后是"粘性"的——聚合逻辑不能把它拉回 ACTIVE。这防止了"正在卸载但被新请求拉活"这种在生产环境中非常常见的竞态问题。

状态机本身是**无锁的**，基于 `AtomicReference.compareAndSet` 实现，转换失败时返回三种结果：SUCCESS、CONFLICT（CAS 竞争失败）、ILLEGAL（非法转换）。调用者可以精确知道失败原因。

### 2. 三层 ClassLoader 隔离

这是灵珑最核心的技术基础。

```
LingCore ClassLoader (宿主应用)
       │ parent
       ▼
SharedApiClassLoader (共享接口/DTO 契约层，全局唯一)
       │ parent
       ▼
LingClassLoader (每个灵元独立，Child-First)
```

**SharedApiClassLoader** 是一个全局单例，只加载灵元之间需要共享的接口和 DTO。它有一个"边界冻结"机制：启动完成后，不允许再添加新的 JAR 或包。这防止了"一半灵元看到旧接口、一半看到新接口"的不一致问题。

**LingClassLoader** 采用 Child-First 策略——每个灵元有自己的实现类副本。但 `java.*`、`com.lingframe.api.*`、共享 API 包等始终走父委派，防止 `ClassCastException` 和 `LinkageError`。

一个容易踩的坑：Windows 下 JAR 文件的 URL 连接缓存会导致文件句柄泄漏，阻止热重载时覆盖 JAR 文件。LingClassLoader 在构造时禁用 JAR URL 连接缓存来解决这个问题。

另一个关键细节：`InvocationContext` 中参数类型用 `String[]` 而非 `Class<?>[]` 存储，持有 `LingRuntime` 用 `WeakReference` 而非强引用。这些看起来不起眼的设计，都是为了防止核心线程意外持有灵元的 ClassLoader 引用，导致卸载后无法 GC 回收。

### 3. 十级调用管道

灵珑的每次服务调用都经过一条过滤器链，按顺序执行：

| 阶段 | 过滤器 | 职责 |
|------|--------|------|
| 指标 | TrafficMetricsFilter | 记录请求数和延迟 |
| 状态守卫 | MacroStateGuardFilter | 运行时非 ACTIVE 则拒绝 |
| 路由 | CanaryRoutingFilter | 选择目标实例（灰度/稳定） |
| 韧性 | ResilienceGovernanceFilter | 限流 + 熔断 |
| 解析 | ContextIsolationFilter | ClassLoader/方法解析 |
| 治理 | GovernanceDecisionFilter | 聚合治理规则 |
| 权限 | PermissionGovernanceFilter | 零信任权限校验 |
| 执行隔离 | ThreadIsolationGovernanceFilter | 线程池隔离 |
| 终端 | TerminalInvokerFilter | 实际方法调用 |

这条管道在**启动时**会做 `validatePhaseContracts()` 校验——如果任何内置过滤器的顺序不对，或者阶段约束被违反，应用直接启动失败。不会等到生产流量来了才发现问题。

`InvocationContext` 使用 ThreadLocal 对象池，避免高频分配。状态分为四个分区对象（路由、解析、治理、执行），不是传统的 String-keyed attachment——类型安全，不容易出错。

### 4. 写权限纪律

整个框架对"谁能修改什么"有严格的约束：

- `StateMachine.transition()` 只存在于 StateMachine 自身
- `InstanceCoordinator` 是唯一有权驱动实例状态转换的角色
- `RuntimeCoordinator` 是唯一持有和写入运行时状态机的角色
- `InstancePool` 用私有锁对象（不是 `this`）序列化所有成员变更操作
- 配置更新用 volatile 引用替换，读操作永远无锁

每个可变概念都有且只有一个授权写入者，层与层之间通过事件和快照通信，不直接修改对方的状态。

---

## 性能：没专门优化，但数据还不错

我优先解决的是稳定性和确定性问题，没有专门做过性能调优。但跑完 JMH 基准测试后，数据比我预期的好：

**端到端稳态调用**

| 线程数 | 吞吐量 (ops/us) | 延迟 (us/op) | 内存分配 (B/op) |
|--------|-----------------|-------------|----------------|
| 1T | 0.049 | 19.39 | 0.209 |
| 4T | 0.167 | 22.03 | 0.109 |
| 8T | 0.338 | 22.39 | 0.096 |

8 线程相比 1 线程实现 6.9 倍加速（86% 效率），延迟仅增加 15%。

**核心组件吞吐量**

| 组件 | 吞吐量 | 内存分配 |
|------|--------|---------|
| EventBus（无监听者） | 198.8M ops/sec | ~10⁻⁴ B/op |
| EventBus（Ling 监听者） | 40.0M ops/sec | ~10⁻³ B/op |
| Repository 查找（8T） | 716.9M ops/sec | ~10⁻⁴ B/op |
| StateMachine 并发读（8T） | 2,323.7M ops/sec | ~10⁻⁵ B/op |
| FilterRegistry 缓存命中 | 188.0M ops/sec | ~10⁻⁴ B/op |

**线程扩展效率**

| 组件 | 4T 效率 | 8T 效率 |
|------|---------|---------|
| EventBus 并发发布 | 94% | 88% |
| StateMachine 并发读 | 87% | 85% |
| Repository 查找 | 85% | 84% |
| EndToEnd 稳态 | 85% | 86% |

热路径操作几乎全部零分配（10⁻⁴ B/op 级别），意味着即使在高吞吐下也几乎不会触发 GC。

**ClassLoader 生命周期**

创建和销毁操作对 JAR 数量几乎不敏感（22-34 us），说明类加载器管理本身是 O(1) 的。包含类加载的完整路径随 JAR 数量线性增长，这是 JVM 类加载的固有开销。

坦白说，这些数据在没有专门优化的前提下，跟 Akka（~5000 万消息/秒）和 LMAX Disruptor（~2500 万 ops/sec）处于同一量级。说明架构底子好——当你选择了正确的数据结构和并发模型，性能是自然来的。

---

## 我是怎么做出来的

这是我最想分享的部分。

半年前我没用过 ClassLoader，架构设计也只私下练手过一两个小项目。灵珑是我真正意义上的第一个"大"项目。

整个过程大量借助了 AI。但我发现，AI 能帮你写代码，但它不能帮你做架构决策。"我需要两层状态机，一层事实一层意图，通过事件通信"——这个想法是我想出来的，不是 AI 建议的。"ClassLoader 需要边界冻结机制来防止运行时篡改"——这个判断是我在做的时候意识到的。"InvocationContext 里的参数类型必须用 String 不能用 Class，否则会泄漏 ClassLoader"——这种细节是你在理解问题本质之后自然会做出的选择。

AI 改变的是实现速度。以前想清楚一个设计之后，编码实现可能要几天；现在想清楚之后，几个小时就能变成可运行的代码。但"想清楚"这件事，AI 替代不了。

我的经验是：**AI 放大了你的判断力，而不是替代它。** 你知道什么是好的设计，AI 帮你快速实现它。你不知道什么是好的设计，AI 给你的也只是"看起来对"的东西。

---

## 关于开源和信任

灵珑从第一行代码就开源了。0.2 版本的时候，开源中国创始人红薯 star 了项目。0.3 版本后，我被邀请进了 Apache SeaTunnel 核心社群。

但说实话，这些并没有转化成用户。

我后来想明白了一件事：**技术认可和采用决策是两件完全不同的事。** 一个人可以觉得你的代码写得好，但让他把生产系统建立在你上面，他需要的是"出了问题有人兜底"的安全感。这个东西不是 benchmark 数据和测试覆盖率能给的——它只能靠真实的生产案例来积累。

这是一个所有新开源项目都面临的死循环：没有用户就没有案例，没有案例就没有用户。

---

## 下一步

灵珑运行时本身我已经比较有信心了。现在的方向是在它上面构建 lingai——一个 AI 驱动的"一句话需求到可用接口"的产品。灵珑的运行时治理能力（热部署、模块隔离、零信任权限）恰好是让 AI 生成的代码安全跑在生产环境里所需要的基础设施。

这条路还很长。但技术层面该做的准备，已经做好了。

---

*灵珑 GitHub: [https://github.com/lingframe/lingframe](https://github.com/lingframe/lingframe)*

*如果你也在做独立开源项目，或者对 JVM 运行时治理感兴趣，欢迎交流。*
